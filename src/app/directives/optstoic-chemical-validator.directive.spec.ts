import { OptstoicChemicalValidatorDirective } from './optstoic-chemical-validator.directive';

describe('OptstoicChemicalValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OptstoicChemicalValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
